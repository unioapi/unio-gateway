// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: channel_cost_multipliers.sql

package sqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const findActiveChannelCostMultiplier = `-- name: FindActiveChannelCostMultiplier :one
SELECT id, channel_id, model_id, multiplier, status, effective_from, effective_to, created_at, updated_at, model_key
FROM channel_cost_multipliers
WHERE channel_id = $1
    AND (model_id = $2 OR model_id IS NULL)
    AND status = 'enabled'
    AND effective_from <= $3
    AND (
        effective_to IS NULL
        OR effective_to > $3
    )
ORDER BY (model_id IS NULL) ASC, effective_from DESC, id DESC
LIMIT 1
`

type FindActiveChannelCostMultiplierParams struct {
	ChannelID int64
	ModelID   pgtype.Int8
	AtTime    pgtype.Timestamptz
}

// FindActiveChannelCostMultiplier 查找指定 channel/model 在指定时间生效的价格倍率：优先逐模型覆盖，
// 无则回退渠道默认（model_id IS NULL）。(model_id IS NULL) ASC 让覆盖（false=0）排在默认（true=1）之前。
func (q *Queries) FindActiveChannelCostMultiplier(ctx context.Context, arg FindActiveChannelCostMultiplierParams) (ChannelCostMultiplier, error) {
	row := q.db.QueryRow(ctx, findActiveChannelCostMultiplier, arg.ChannelID, arg.ModelID, arg.AtTime)
	var i ChannelCostMultiplier
	err := row.Scan(
		&i.ID,
		&i.ChannelID,
		&i.ModelID,
		&i.Multiplier,
		&i.Status,
		&i.EffectiveFrom,
		&i.EffectiveTo,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.ModelKey,
	)
	return i, err
}

const getChannelCostMultiplier = `-- name: GetChannelCostMultiplier :one
SELECT id, channel_id, model_id, multiplier, status, effective_from, effective_to, created_at, updated_at, model_key FROM channel_cost_multipliers WHERE id = $1 LIMIT 1
`

// GetChannelCostMultiplier 按主键读取单条价格倍率。
func (q *Queries) GetChannelCostMultiplier(ctx context.Context, id int64) (ChannelCostMultiplier, error) {
	row := q.db.QueryRow(ctx, getChannelCostMultiplier, id)
	var i ChannelCostMultiplier
	err := row.Scan(
		&i.ID,
		&i.ChannelID,
		&i.ModelID,
		&i.Multiplier,
		&i.Status,
		&i.EffectiveFrom,
		&i.EffectiveTo,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.ModelKey,
	)
	return i, err
}
