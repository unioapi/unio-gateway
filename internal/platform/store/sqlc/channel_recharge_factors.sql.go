// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: channel_recharge_factors.sql

package sqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const findActiveChannelRechargeFactor = `-- name: FindActiveChannelRechargeFactor :one
SELECT id, channel_id, factor, status, effective_from, effective_to, created_at, updated_at
FROM channel_recharge_factors
WHERE channel_id = $1
    AND status = 'enabled'
    AND effective_from <= $2
    AND (
        effective_to IS NULL
        OR effective_to > $2
    )
ORDER BY effective_from DESC, id DESC
LIMIT 1
`

type FindActiveChannelRechargeFactorParams struct {
	ChannelID int64
	AtTime    pgtype.Timestamptz
}

// FindActiveChannelRechargeFactor 查找指定 channel 在指定时间生效的充值倍率（账户级、无 model 维度）。
func (q *Queries) FindActiveChannelRechargeFactor(ctx context.Context, arg FindActiveChannelRechargeFactorParams) (ChannelRechargeFactor, error) {
	row := q.db.QueryRow(ctx, findActiveChannelRechargeFactor, arg.ChannelID, arg.AtTime)
	var i ChannelRechargeFactor
	err := row.Scan(
		&i.ID,
		&i.ChannelID,
		&i.Factor,
		&i.Status,
		&i.EffectiveFrom,
		&i.EffectiveTo,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getChannelRechargeFactor = `-- name: GetChannelRechargeFactor :one
SELECT id, channel_id, factor, status, effective_from, effective_to, created_at, updated_at FROM channel_recharge_factors WHERE id = $1 LIMIT 1
`

// GetChannelRechargeFactor 按主键读取单条充值倍率。
func (q *Queries) GetChannelRechargeFactor(ctx context.Context, id int64) (ChannelRechargeFactor, error) {
	row := q.db.QueryRow(ctx, getChannelRechargeFactor, id)
	var i ChannelRechargeFactor
	err := row.Scan(
		&i.ID,
		&i.ChannelID,
		&i.Factor,
		&i.Status,
		&i.EffectiveFrom,
		&i.EffectiveTo,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
