version: "2"
sql:
  - engine: postgresql
    schema: "migrations/*.up.sql"
    queries: sql/queries
    gen:
      go:
        package: sqlc
        out: internal/platform/store/sqlc
        sql_package: pgx/v5
